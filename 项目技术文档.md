# Bloom Era Sim 项目技术文档

## 1. 项目概述

Bloom Era Sim（青春纪元模拟器）是一款基于React和TypeScript开发的生活模拟游戏，玩家可以在游戏中体验学生时代的各种挑战和选择。

### 1.1 核心功能

- **多难度游戏模式**：普通、中等、现实三种难度级别
- **天赋系统**：玩家可以选择不同的天赋来增强游戏体验
- **事件系统**：随机事件和选择影响游戏进程
- **考试系统**：定期考试评估玩家表现
- **多UI风格**：默认、液态玻璃、亚克力三种视觉风格
- **成就系统**：记录玩家游戏成就
- **数据持久化**：本地存储用户数据

### 1.2 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 19.2.3 | 前端框架 |
| TypeScript | 5.2.2 | 类型系统 |
| Vite | 5.2.0 | 构建工具 |
| ESLint | 8.57.0 | 代码质量 |

## 2. 项目结构

```
Bloom-Era-Sim-main/
├── public/              # 静态资源
├── src/
│   ├── components/      #  React 组件
│   ├── data/            # 游戏数据和常量
│   ├── hooks/           # 自定义 React 钩子
│   ├── lib/             # 工具库
│   ├── App.tsx          # 应用主组件
│   ├── main.tsx         # 应用入口
│   └── types.ts         # TypeScript 类型定义
├── package.json         # 项目配置
├── tsconfig.json        # TypeScript 配置
└── vite.config.ts       # Vite 配置
```

### 2.1 核心文件说明

| 文件/目录 | 职责 | 关键功能 |
|-----------|------|----------|
| `src/App.tsx` | 应用主组件 | 页面管理、模态窗口控制、全局状态 |
| `src/hooks/useGameLogic.ts` | 游戏逻辑钩子 | 游戏状态管理、事件处理、进度控制 |
| `src/data/mechanics.ts` | 游戏机制定义 | 天赋、商店、社团、周末活动 |
| `src/data/constants.ts` | 游戏常量 | 难度设置、游戏规则、UI配置 |
| `src/types.ts` | 类型定义 | 全局 TypeScript 类型 |

## 3. 核心系统设计

### 3.1 游戏状态管理

游戏状态通过 `useGameLogic` 钩子集中管理，使用 React 的 `useState` 和 `useCallback` 实现状态管理和性能优化。

**核心状态**：
- `currentPage`：当前游戏页面
- `playerState`：玩家状态（心态、健康、学术等）
- `currentEvent`：当前事件
- `examResults`：考试结果
- `isGameOver`：游戏结束状态

**关键方法**：
- `handleDifficultySelect`：处理难度选择
- `handleTalentSelect`：处理天赋选择
- `handleEventChoice`：处理事件选择
- `advanceGameDay`：推进游戏天数
- `handleExamComplete`：处理考试完成

### 3.2 页面系统

游戏包含多个页面，通过 `currentPage` 状态控制显示：

| 页面 | 组件 | 功能 |
|------|------|------|
| `home` | `HomeView` | 主页、难度选择 |
| `talent` | `TalentView` | 天赋选择 |
| `game` | 内置组件 | 游戏主界面（开发中） |
| `exam` | `ExamView` | 考试界面 |
| `ending` | `EndingScreen` | 游戏结局 |

### 3.3 事件系统

## 事件发生逻辑（简洁专业版）

### 1. 事件触发机制

**核心流程**
1. **前置检查**：验证游戏状态（非暂停、非周末）
2. **队列检查**：优先处理已存在事件队列
3. **阶段检查**：验证阶段转换或固定事件节点
4. **事件生成**：按优先级生成新事件
5. **事件激活**：设置为当前事件，暂停时间推进

**触发条件**
- **时间触发**：基于游戏内"周"单位的时间推进
- **状态触发**：基于玩家属性或游戏进度变化
- **阶段触发**：基于当前游戏阶段的特定事件池

### 2. 事件优先级系统（从高到低）

1. **固定事件**（FIXED）
   - 特性：特定阶段和周数必然触发，不可跳过
   - 示例：分班考试、期中/期末考试

2. **条件事件**（CONDITIONAL）
   - 特性：满足特定条件时触发，基于游戏状态动态判断
   - 示例：负债事件（金钱<0）、健康危机（健康<20）

3. **随机事件**（RANDOM）
   - 特性：从对应阶段事件池随机选择，防重复触发
   - 示例：日常学习、社交、生活事件

### 3. 事件生成系统

**事件池设计**
- **阶段事件池**：每个阶段对应独立事件池
- **事件分类**：按类型（正面/负面/中性）和主题（学习/社交/生活）分类
- **权重系统**：基于玩家状态调整事件触发概率

**生成算法**
1. **阶段筛选**：选择对应阶段事件池
2. **条件筛选**：过滤满足当前状态的事件
3. **去重筛选**：排除最近触发的事件
4. **概率选择**：基于权重随机选择最终事件

### 4. 事件处理流程

1. **展示阶段**
   - 渲染事件标题、描述和可用选择项
   - 基于游戏状态过滤不可用选择

2. **选择阶段**
   - 执行选择的action函数，计算状态变化
   - 生成属性变化的视觉反馈
   - 应用计算后的状态变化

3. **确认阶段**
   - 显示选择结果和属性变化
   - 检查是否触发后续事件（事件链）
   - 标记当前事件完成，继续游戏流程

### 5. 事件与游戏状态交互

**状态影响机制**
- **直接属性修改**：修改玩家属性（心态、健康、金钱等）
- **状态效果添加**：添加临时/永久状态效果（BUFF/DEBUFF）
- **关系状态变化**：影响与NPC的关系
- **成就触发检查**：基于选择结果检查成就解锁条件

**状态反馈机制**
- **动态描述**：事件描述根据当前状态调整
- **选择可用性**：基于状态过滤选择项
- **结果差异化**：相同选择在不同状态下产生不同结果

### 6. 系统集成

- **与阶段系统**：每个阶段对应独立事件池，阶段转换触发特定事件
- **与周末系统**：事件处理完成后自动进入周末，周末结果可能触发后续事件
- **与成就系统**：事件结果触发成就解锁检查，为特定成就设计专门事件

### 7. 设计原则

- **一致性**：事件逻辑与游戏整体设计保持一致
- **多样性**：确保事件类型和结果的多样性
- **平衡性**：正面和负面事件的合理分布
- **相关性**：事件内容与当前阶段和玩家状态相关
- **可扩展性**：便于添加新事件和事件类型

### 8. 核心流程图

```
[游戏主循环] → [事件触发检查] → [队列检查] → [阶段检查] → [事件生成] → [事件激活]
    ↑                                                           ↓
[事件完成] ← [结果确认] ← [选择处理] ← [事件展示] ← [事件渲染]
```

### 3.4 天赋系统

天赋系统允许玩家在游戏开始时选择天赋，增强特定属性：

| 天赋 | 效果 | 消耗点数 |
|------|------|----------|
| 快速学习 | 学习效率提高20% | 3 |
| 意志力强 | 心态提高10点 | 2 |
| 社交达人 | 社交提高15点 | 2 |
| 健康生活 | 健康提高10点 | 2 |
| 勤俭节约 | 金钱增加50 | 1 |

### 3.5 难度系统

游戏提供三种难度级别，影响初始属性和游戏体验：

| 难度 | 初始属性 | 天赋点数 | 事件频率 |
|------|----------|----------|----------|
| 普通 | 心态80, 健康80, 学术60, 社交60, 金钱300 | 3 | 1.0 |
| 中等 | 心态60, 健康60, 学术50, 社交50, 金钱200 | 2 | 1.3 |
| 现实 | 心态40, 健康40, 学术40, 社交40, 金钱100 | 1 | 1.5 |

### 3.6 UI风格系统

游戏支持三种UI风格，通过 CSS 变量和条件样式实现：

| 风格 | 特点 | 实现方式 |
|------|------|----------|
| 默认 | 标准白色背景 | 常规 CSS 样式 |
| 液态玻璃 | 半透明模糊效果 | `backdrop-filter: blur()` |
| 亚克力 | 高透明度模糊 | 更强的 `backdrop-filter` |

## 4. 核心组件实现

### 4.1 App 组件

`App` 组件是应用的主入口，负责：
- 整合游戏逻辑钩子
- 管理页面渲染
- 控制模态窗口
- 处理用户昵称输入
- 应用全局样式和背景效果

**关键代码**：
```typescript
function App() {
  // 使用游戏逻辑钩子
  const {
    currentPage,
    showModal,
    currentModal,
    playerState,
    examResults,
    setCurrentPage,
    openModal,
    closeModal,
    handleDifficultySelect,
    handleTalentSelect,
    handleExamComplete,
    handleRestart,
    handleMainMenu,
    advanceGameDay
  } = useGameLogic();

  // 状态管理：用户昵称
  const [userName, setUserName] = useState<string | null>(null);
  const [showNameInput, setShowNameInput] = useState(false);
  const [nicknameInput, setNicknameInput] = useState('');
  
  // 状态管理：UI风格
  const [uiStyle, setUiStyle] = useState<string>('default');

  // 渲染不同页面
  const renderPage = () => {
    switch (currentPage) {
      case 'home':
        return <HomeView ... />;
      case 'talent':
        return <TalentView ... />;
      case 'exam':
        return <ExamView ... />;
      case 'ending':
        return <EndingScreen ... />;
      case 'game':
        return <div className="game-content">...</div>;
      default:
        return <HomeView ... />;
    }
  };

  return (
    <div className="app-container">
      {/* 背景几何图形 */}
      <div className="bg-shapes">...</div>
      
      {/* 全局设置按钮 */}
      <button className="global-settings-button" onClick={() => openModal('settings')}>⚙️</button>
      
      {/* 渲染当前页面 */}
      {renderPage()}
      
      {/* 模态窗口 */}
      {showModal && currentModal === 'settings' && <SettingsModal ... />}
      {/* 其他模态窗口 */}
    </div>
  );
}
```

### 4.2 游戏逻辑钩子

`useGameLogic` 钩子是游戏的核心，实现了游戏的主要逻辑：

**功能**：
- 游戏状态初始化和管理
- 难度和天赋选择处理
- 事件生成和处理
- 游戏进度控制
- 考试系统

**关键代码**：
```typescript
export const useGameLogic = (): UseGameLogicReturn => {
  // 状态管理
  const [currentPage, setCurrentPage] = useState<PageType>('home');
  const [playerState, setPlayerState] = useState<PlayerState>(initialPlayerState);
  const [currentEvent, setCurrentEvent] = useState<GameEvent | null>(null);
  const [examResults, setExamResults] = useState<ExamResult[]>([]);
  const [isGameOver, setIsGameOver] = useState(false);

  // 处理难度选择
  const handleDifficultySelect = useCallback((difficultyName: string) => {
    const difficulty = difficultySettings.find(d => d.name === difficultyName);
    if (difficulty) {
      setPlayerState({
        ...initialPlayerState,
        mental: difficulty.starting_stats.mental,
        health: difficulty.starting_stats.health,
        academic: difficulty.starting_stats.academic,
        social: difficulty.starting_stats.social,
        money: difficulty.starting_stats.money,
        talent_points: difficulty.talent_points,
        difficulty: difficulty.name
      });
      setCurrentPage('talent');
    }
  }, []);

  // 推进游戏天数
  const advanceGameDay = useCallback(() => {
    setPlayerState(prev => {
      const newDay = prev.game_day + 1;
      
      // 每天的自然衰减和恢复
      const newState = {
        ...prev,
        game_day: newDay,
        mental: Math.max(0, Math.min(100, prev.mental + 2 - 1)),
        health: Math.max(0, Math.min(100, prev.health + 1 - 0.5)),
        academic: Math.max(0, prev.academic - 0.5),
        social: Math.max(0, prev.social - 0.5),
        money: Math.max(0, prev.money - 5)
      };
      
      // 检查游戏结束条件
      if (newDay > 365) {
        setIsGameOver(true);
        setCurrentPage('ending');
      }
      
      // 随机事件触发
      if (Math.random() < 0.3 && !currentEvent) {
        const randomEvent = generateRandomEvent();
        setCurrentEvent(randomEvent);
      }
      
      // 考试触发
      if (newDay % 30 === 0) {
        setCurrentPage('exam');
      }
      
      return newState;
    });
  }, [currentEvent]);

  return {
    // 状态和方法
  };
};
```

### 4.3 主页组件

`HomeView` 组件是游戏的入口页面，提供：
- 游戏标题和欢迎信息
- 难度选择
- 侧边功能按钮（成就、版本、设置、关于、GitHub）
- 开始游戏按钮

**特点**：
- 响应式设计
- 动画效果（标题颜色变化）
- 多UI风格支持
- 交互反馈

## 5. 技术实现细节

### 5.1 状态管理

项目使用 React 的内置 `useState` 和 `useCallback` 钩子实现状态管理，没有使用 Redux 等状态管理库。这种方式适用于中小型应用，简化了代码结构。

### 5.2 性能优化

- 使用 `useCallback` 缓存回调函数，避免不必要的重新渲染
- 组件拆分合理，减少大型组件的重渲染
- 状态更新使用函数式更新，确保状态的一致性

### 5.3 本地存储

游戏使用 `localStorage` 存储用户昵称，实现数据持久化：

```typescript
// 初始化：检查localStorage中的用户昵称
useEffect(() => {
  const savedName = localStorage.getItem('bloomEraSimUserName');
  if (savedName) {
    setUserName(savedName);
  } else {
    setShowNameInput(true);
  }
}, []);

// 处理保存用户昵称
const handleSaveName = (name: string) => {
  if (name.trim()) {
    setUserName(name.trim());
    localStorage.setItem('bloomEraSimUserName', name.trim());
    setShowNameInput(false);
  }
};
```

### 5.4 动画效果

项目使用 CSS 动画实现各种视觉效果：

- 标题颜色变化动画
- 按钮悬停效果
- 模态窗口过渡
- 背景几何图形

### 5.5 响应式设计

项目使用 CSS 媒体查询实现响应式设计，适配不同屏幕尺寸：

```css
@media (max-width: 768px) {
  .home-header h1 {
    font-size: 2.5rem;
  }
  
  .difficulty-cards {
    grid-template-columns: 1fr;
  }
}
```

## 6. 游戏流程

### 6.1 游戏开始流程

1. **启动游戏** → 显示主页
2. **输入昵称** → 首次启动时
3. **选择难度** → 普通/中等/现实
4. **选择天赋** → 根据难度获得不同点数
5. **进入游戏** → 开始游戏主界面

### 6.2 游戏主流程

1. **日常活动** → 学习、社交、休息等
2. **随机事件** → 处理各种事件和选择
3. **周末活动** → 选择周末活动
4. **定期考试** → 每30天一次考试
5. **社团活动** → 加入和参与社团
6. **游戏结束** → 365天后显示结局

### 6.3 结局流程

1. **显示最终统计** → 各项属性值
2. **显示成就** → 解锁的成就
3. **显示考试结果** → 历次考试成绩
4. **重新开始** → 回到主页
5. **主菜单** → 回到主页

## 7. 未来开发计划

### 7.1 功能完善

- **游戏主界面**：完成游戏主界面的开发
- **事件系统**：增加更多事件和选择
- **社团系统**：完善社团功能和互动
- **商店系统**：实现物品购买和使用
- **周末活动**：增加更多周末活动选项

### 7.2 技术改进

- **性能优化**：进一步优化渲染性能
- **代码组织**：优化代码结构和命名
- **测试覆盖**：增加单元测试和集成测试
- **国际化**：支持多语言

### 7.3 内容扩展

- **OI竞赛系统**：实现编程竞赛相关内容
- **多结局**：根据玩家选择和属性值生成不同结局
- **成就系统**：增加更多成就和解锁条件
- **排行榜**：实现玩家排行榜（需要后端）

## 8. 项目亮点

1. **现代化技术栈**：使用 React 19 和 TypeScript，代码质量高
2. **模块化设计**：组件和逻辑分离，易于维护和扩展
3. **丰富的游戏机制**：天赋、事件、考试等多种游戏系统
4. **精美的UI设计**：多种UI风格和动画效果
5. **响应式布局**：适配不同设备屏幕
6. **良好的用户体验**：流畅的动画和交互反馈
7. **可扩展性**：模块化设计便于添加新功能

## 9. 总结

Bloom Era Sim 是一个设计良好、功能丰富的生活模拟游戏项目，使用现代前端技术栈实现。项目结构清晰，代码组织合理，具有良好的可维护性和可扩展性。

**技术特点**：
- 使用 React 19 和 TypeScript 确保代码质量
- 模块化设计便于功能扩展
- 响应式布局适配不同设备
- 多种UI风格和动画效果
- 本地存储实现数据持久化

**游戏特色**：
- 丰富的游戏机制（天赋、事件、考试等）
- 多种难度级别
- 随机事件系统
- 成就系统
- 多结局可能性

该项目展示了如何使用现代前端技术构建一个完整的游戏应用，是学习 React、TypeScript 和前端游戏开发的优秀案例。